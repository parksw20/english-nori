# 영어놀이 — 7세용 영어 학습 웹앱 설계 가이드 (v1.0 확정, 2026-08-17)

> 자매 프로젝트: `C:\CLI\PROJECT\한자놀이`(https://parksw20.github.io/hanja-nori/).
> 그 프로젝트에서 검증된 구조(공식 기준 = 게임 구조, 손입력 데이터 최소화 + 무결성 테스트,
> SRS, PWA, Pages 배포, 데이터 테스트 실패 시 배포 중단)를 그대로 재사용한다.

---

## 1. 핵심 설계 원칙 — 4가지

### (1) 파닉스 단계 = 스테이지, 단계 테스트 = 보스전
한자놀이의 "급수 = 스테이지"에 대응하는 것은 **급수가 아니라 파닉스 단계(synthetic phonics)**다.
파닉스는 이미 완성된 난이도 곡선(소리 → 조합 → 예외)이므로 **난이도 밸런싱을 하지 않는다.**
7세(초1)는 정규 영어 교육과정(초3 시작) 이전이므로 교육과정 어휘표는 상위 목표로만 쓰고,
실제 사다리는 파닉스가 정본이 된다.

### (2) 읽기 이전 아이 = 오디오가 1급 시민
7세는 **영어 글자도, 한글 설명문도 아직 못 읽는 상태**를 기본값으로 가정한다.
- 모든 화면은 **그림 + 소리**로 성립해야 한다. 한글 안내문은 보조.
- 낱말·문항 지시는 전부 TTS(en-US)로 읽어 준다. 한자놀이 `src/tts.ts`(음성 우선순위 선택) 재사용.
- 오답에 벌점·빨간 X를 주지 않는다. 즉시 **정답 소리를 다시 들려주고** 넘어간다.

### (3) 손으로 넣는 데이터는 최소, 나머지는 계산
손입력 정본: **① 파닉스 단계표(그래핌→소리) ② 낱말 목록(스펠링·뜻·그림키·단계) ③ sight words ④ 문장 템플릿**
계산해서 만드는 것: 문제 4지선다 보기, 오답 후보(같은 단계 안 최소 대조쌍 minimal pair 우선),
단어의 그래핌 분해, 단계별 누계, 스테이지 테스트 구성, 발음 오디오(TTS).

### (4) 하루 10~15분에 끝나는 세션
7세 집중력 기준. "오늘의 놀이" = 새 낱말 4~6개 + 복습 8~10개 + 미니게임 1판.
끝나면 스티커 1장 + 부모용 한 줄 리포트. 무한 플레이는 만들지 않는다.

---

## 2. 커리큘럼 사다리 (초안 — 착수 시 공식 출처로 확정)

| 단계 | 이름 | 배우는 소리/규칙 | 대표 낱말 | 누계 낱말(목표) |
|---|---|---|---|---|
| 0 | 알파벳 | A~Z 이름 + 대표 소리 26 | apple, ball… | 26 |
| 1 | 단모음 CVC | a / i / o / u / e | cat, pig, dog, bus, bed | +60 (86) |
| 2 | 끝자음군·이중자음 | -ck -ll -ss, bl br cl cr… | duck, frog, clap | +50 (136) |
| 3 | 다이그래프 | sh ch th ng wh | fish, chair, thumb | +40 (176) |
| 4 | 매직 e | a_e i_e o_e u_e | cake, bike, nose | +40 (216) |
| 5 | 장모음 팀 | ai ay ee ea oa ow | rain, tree, boat | +45 (261) |
| 6 | r-모음·이중모음 | ar or er ir ur / oo ou oi | car, star, moon | +45 (306) |
| S | Sight words 트랙 | 파닉스로 안 되는 고빈도어 | the, is, you, said | 별도 100~120 |

- **어휘 범위의 상위 정본 후보**: Cambridge YLE **Starters(Pre A1)** 공식 wordlist(약 450 낱말) 또는
  2022 개정 교육과정 초등 권장 어휘. 둘 중 하나를 정해 "우리 낱말은 이 목록 안에 있다"를 테스트로 강제한다.
- **Sight words 정본 후보**: Dolch pre-primer/primer/1st, 또는 Fry first 100.
- ⚠️ **한자놀이 교훈**: 공식 표를 크롤링하지 못하거나 값이 깨져 오면 **추측해서 채우지 말고 "미확정"으로 표시**한다.
  추출 결과는 공식 합계(단계별 개수·누계)와 대조해 일치할 때만 확정한다.

---

## 3. 놀이 구조

- **여행 지도** — 단계별 마을. 그 단계의 보스전(단계 테스트)을 통과해야 다음 마을이 열린다.
- **낱말 도감(SRS)** — 익힌 낱말이 흑백 카드 → 컬러 → 반짝임으로 자란다. 복습을 미루면 색이 빠진다.
  (한자놀이 `srs.ts` = SM-2 축소판 그대로 재사용. 메타포만 정원 → 도감으로 교체)
- **미니게임 (MVP 3종 + 확장)**
  1. `소리 사냥` — 소리를 듣고 글자/그래핌 고르기 (단계 0~1의 핵심)
  2. `낱말 만들기` — 그림을 보고 글자 타일을 끌어 스펠링 완성 (c·a·t)
  3. `그림 짝맞추기` — 낱말 카드 ↔ 그림 뒤집기 (누계 낱말에서만 출제)
  4. (확장) `읽고 잡기` — sight word를 빠르게 탭하는 요격형
  5. (확장) `말하기` — 그림 보고 발음 → Web Speech 인식 채점
  6. (확장) `듣고 고르기` — 짧은 문장 듣고 그림 선택 (Starters Listening 형태)
- **스테이지 테스트(보스)** = 그 단계 누계에서 출제. 문항 유형은 고정 구성표로 못 박고
  합격선 70%(한자놀이와 동일 기준). 합격 시 **수료증 + 스티커**.
- **보상은 하한선~만점 구간에 분배** (교훈: 성과 비례 보상은 0이 아니라 합격선부터 만점까지 나눈다).

---

## 4. 기술 스택 (한자놀이와 동일 — 검증된 조합)

```
Vite + TypeScript (프레임워크 없음), localStorage 저장, PWA(오프라인), GitHub Pages 자동배포
npm test = 데이터 무결성 테스트 (실패 시 배포 중단)
dev/preview 포트: 5310 고정 (strictPort. 5300은 한자놀이가 쓴다)
```

```
src/
  data/phonics.ts     단계표: 그래핌 → 소리(IPA/한글근사) → 예시 낱말   ← 손입력 정본
  data/words.ts       낱말: spelling, ko, emoji/그림키, stage, graphemes? ← 손입력 정본
  data/sight.ts       sight words (Dolch/Fry 단계별)                    ← 손입력 정본
  data/sentences.ts   문장 템플릿 (확장 단계)                            ← 손입력 정본
  phonics.ts          낱말 → 그래핌 분해, 단계 적합성 판정 (계산)
  srs.ts              간격 반복 = 낱말 도감
  progress.ts         수료증·최고기록·스티커
  exam.ts             단계 테스트 생성 + 문제 유형 생성기 (계산)
  distract.ts         오답 보기 생성 — minimal pair 우선 (계산)
  tts.ts              en-US 음성 (한자놀이에서 이식, 음성 우선순위 선택)
  speech.ts           (확장) Web Speech 발음 인식
  games/              soundhunt.ts · wordbuild.ts · match.ts …
  report.ts           부모용 한 줄 리포트
test/data.test.ts     데이터·출제기준 무결성 테스트
```

### 그림 자산
1순위 **이모지**(무료·즉시·라이선스 무관, 구체 명사 대부분 커버) → 2순위 **OpenMoji SVG 번들**(CC BY-SA, 통일감)
→ 3순위 `ImageFactory`로 AI 생성(추상어·동작어). MVP는 1순위로 끝내고, 도감 완성도용으로 뒤에 교체.

### 소리
- 낱말·문장: **브라우저 TTS(en-US)**. Windows Natural 음성이 있으면 우선 선택(한자놀이 로직 이식).
- 파닉스 개별 음소(/æ/, /sh/)는 TTS가 부정확할 수 있다 → **단어 안에서 들려주는 방식**을 기본으로 하고,
  음소 단독 발음은 짧은 오디오 파일이 필요한지 착수 후 실측해 판단한다. (미확정 항목)

---

## 5. 무결성 테스트 (착수 즉시 만든다)

한자놀이 교훈의 직접 이식 — 손으로 넣는 카탈로그 데이터에는 반드시 테스트를 붙인다.
1. 낱말의 모든 글자가 **그 단계까지 배운 그래핌으로만** 이루어졌는가 (단계 침범 금지)
2. 각 그래핌이 최소 3개 낱말에 등장하는가 (연습량 보장)
3. 단계별 낱말 수·누계가 표와 일치, 단계 간 중복 없음
4. 모든 낱말에 그림키(이모지/자산)와 한국어 뜻이 있는가
5. sight words가 파닉스 낱말과 충돌하지 않는가
6. **모든 단계의 테스트를 실제로 생성해** 문항 수·정답 포함·4지선다 여부 확인
7. 오답 보기 생성기가 정답과 겹치지 않고, 같은 단계 안에서만 뽑는가

## 6. 검증 계획 (§2.5)
- 브라우저 패널이 안 보이면 스크린샷 포기 → **DOM/JS 실측**(모듈 import로 정답 계산 후 합성 이벤트 클릭).
  뷰포트는 `resize_window`로 375/768/1280 명시하고 가로 넘침 0 확인.
- 오디오는 `muted play + ended 디스패치`로 경로만 확인. **실제 발음 품질·목소리 취향은 사용자(육안·귀) 판단 영역**
  → "미검증"으로 명시 보고한다.
- 서비스 워커는 **localhost에서 등록 금지**, HTML은 네트워크 우선, 해시 자산만 캐시 우선, 캐시명에 버전.

## 7. 단계별 진행안

| 페이즈 | 내용 | 완료 기준 |
|---|---|---|
| P0 | 공식 출처 확정(어휘·sight words) + 단계표/낱말 정본 + 무결성 테스트 | `npm test` 통과, 낱말 86개(단계 0~1) |
| P1 | **MVP**: 지도·도감·TTS·미니게임 3종·말하기·단계 0~1 보스전·진행 저장 | ✅ 2026-08-17 완료 — 브라우저 실측(시험 합격/불합격·세션 완주·게임 3종·반응형) |
| P2 | 단계 2~6 확장 + sight words 트랙 + 스티커/수료증 | 누계 300낱말, 전 단계 보스 생성 테스트 통과 |
| P3 | 말하기(음성인식)·듣고 고르기·문장 만들기 | Chrome 실기기 확인 |
| P4 | PWA 오프라인 + GitHub Pages 공개 + 부모 리포트 | 공개 URL에서 재로드 후 정상 |

## 8. 확정된 결정 (2026-08-17, 사용자)

| 항목 | 결정 |
|---|---|
| MVP 범위 | **단계 0~1** (알파벳 26 소리 + 단모음 CVC), 누계 낱말 **50개** (착수 후 실측 — 아래 참고) |
| 어휘 정본 | **Cambridge YLE Starters (Pre A1)** 공식 wordlist — 우리 낱말은 이 목록 안에 있어야 한다(테스트로 강제) |
| 공개 배포 | **한자놀이와 동일** — 공개 저장소 + GitHub Pages 자동배포(Actions), 데이터 테스트 실패 시 배포 중단 |
| 말하기(음성인식) | **MVP에 포함** (아래 제약·대체경로 필수) |

### 말하기를 MVP에 넣을 때의 제약 — 설계에 미리 박아 둘 것
1. `SpeechRecognition`은 **Chrome/Edge 전용 + 네트워크 필수**다. PWA 오프라인과 충돌하므로
   **기능 감지 후 없으면 숨기고**, 오프라인이면 대체 모드로 자동 전환한다.
2. **대체 경로(항상 동작)**: `MediaRecorder`로 아이 목소리를 녹음 → 원어민 TTS와 **번갈아 재생**해
   스스로 비교하게 한다. 채점 없이도 말하기 연습이 성립하므로 이쪽이 기본, 인식 채점은 보너스.
3. **채점은 관대하게**: 7세 발음 + 아이 목소리에 대한 인식률은 낮다. 정답 판정은
   `transcript`에 목표 낱말이 포함되거나 편집거리 ≤1이면 통과. **실패는 오답이 아니라 "다시 해볼래?"**로만 표시하고
   3회 시도 후에는 무조건 통과시킨다(막히면 놀이가 끝나므로).
4. 마이크 권한은 **말하기 놀이에 처음 들어갈 때만** 요청한다(첫 화면에서 요청 금지).
   HTTPS 필요 → GitHub Pages는 충족, 로컬은 localhost라 충족.
5. 검증 한계: 마이크 입력은 헤드리스로 실측할 수 없다 → **"미검증(사용자 실기기 확인 필요)"으로 보고**한다.

## 8-1. 착수 후 바뀐 사실 (2026-08-17 P0 완료)

- **낱말 수가 86 → 50으로 줄었다.** Pre A1 Starters 목록 안에서 단모음 CVC를 계산해 뽑으니
  40개(고유명사·기능어 포함)뿐이고, 그림으로 낼 수 있는 것은 25개가 한계였다.
  특히 단모음 o는 box·dog **둘뿐**이라 그대로 두면 "낱말 두 개인 마을"이 된다.
  → 기준 이탈을 감추지 않고 `source: 'phonics'` + 이유 + **단계별 30% 상한 테스트**로 드러냈다(현재 7개).
  낱말을 크게 늘리려면 단계 2(쌍자음·이중자음)를 열어야 한다 — sock·duck·frog·clock이 한꺼번에 들어온다.
- **Starters 어휘 정본은 표제어 491개**로 확보(공식 PDF 두 방식 독립 추출 후 합집합).
  공식이 총 개수를 명시하지 않아 **개수 대조는 미확정**으로 남는다.
- **알파벳 단계 대표 낱말은 해독 대상이 아니다**를 설계에 반영했다. apple·umbrella·zoo는 소리를
  기억시키는 그림일 뿐이므로, "그 단계까지 배운 글자" 규칙은 **읽기를 요구하는 문제(`decode`)에만** 적용한다.
  이 구분이 없을 때 단계 0 시험이 umbrella의 스펠링을 묻고 있었다(테스트가 잡았다).

## 9. P0 실행 순서 (완료 — 2026-08-17)

1. `npm create vite` (vanilla-ts) + 포트 5310 고정 + `한자놀이`에서 `tts.ts`·`srs.ts`·`progress.ts` 이식(en-US로 전환)
2. **YLE Starters wordlist 확보** — 공식 Cambridge 페이지에서. 크롤링이 잘리면 추측으로 채우지 말고
   `미확정` 플래그를 데이터에 남긴다(한자놀이 교훈). 확보 후 낱말 수를 공식 합계와 대조.
3. `data/phonics.ts`(단계 0~1 그래핌표) + `data/words.ts`(86 낱말: spelling·ko·emoji·stage) 손입력
4. `test/data.test.ts` — §5의 7개 검사 (단계 침범, 그래핌 최소 3낱말, 누계, 그림/뜻 누락,
   sight words 충돌, 전 단계 보스 생성, 오답 보기 생성기) → `npm test` 통과가 P0 완료 기준
5. 그 다음 P1(지도·도감·미니게임 3종·보스전·말하기)

---
*v1.2 — 2026-08-17: P1 완료. 검증에서 찾은 버그 4건(letter 문제 무음·짝맞추기 입력 잠금·UTC 날짜·음성 0개 기기)을 반영.*
*v1.1 — 2026-08-17: P0 완료. 낱말 수 실측치(50)와 decode 구분을 §8-1에 반영.*
*v1.0 — 2026-08-17: 사용자 결정 4건 반영(MVP 0~1 / YLE Starters / Pages 공개 / 말하기 MVP 포함) 및 말하기 제약 추가.*
*v0.1 — 2026-08-17: 초안 작성. 한자놀이 구조 재사용 + 파닉스 사다리 설계.*
